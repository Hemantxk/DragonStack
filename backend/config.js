const SECONDS = 1000;
const MINUTES = SECONDS * 60;
const HOURS = MINUTES * 60;
const DAYS = HOURS * 24;

const REFRESH_RATES = 5; //unit

module.exports = {
    SECONDS,
    MINUTES,
    HOURS,
    DAYS,
    REFRESH_RATES
};