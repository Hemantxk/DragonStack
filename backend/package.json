{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend project of dragon-stack",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./bin/server",
    "dev": "nodemon ./bin/server",
    "configure": "./bin/configure_db.sh",
    "configure-dev" : "npm run configure && npm run dev"
  },
  "author": "Hemant",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.16"
  },
  "dependencies": {
    "express": "^4.18.1",
    "pg": "^8.7.3"
  }
}
